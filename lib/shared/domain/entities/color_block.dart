/// Fichas de color del juego (capa de dominio: sin dependencias de UI).
///
/// El orden de declaración (ColorId.values) es el orden fijo en que se
/// muestran las fichas en la grilla.
enum ColorId { red, green, blue, yellow }
